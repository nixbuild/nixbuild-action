v25

## Changes

* When OIDC is enabled, a token exchange with the nixbuild.net server is
  performed, to allow for runs outliving the temporary initial OIDC token.

* ci-workflow: Bump nix-quick-install-action version
