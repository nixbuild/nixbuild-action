v20

## Changes

* ci-workflow: Bump `nix-quick-install-action` to the latest release ([v29](https://github.com/nixbuild/nix-quick-install-action/releases/tag/v29)).

* ci-workflow: Fix artifact naming issues.

* ci-workflow: Disable default usage of `tmpfs` for the Nix store (can be enabled manually).
